pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                checkout scm
                echo '✅ Code récupéré depuis GitHub'
            }
        }

        stage('Build Frontend') {
            steps {
                dir('frontend') {
                    // Utiliser /usr/bin/npm (liens symboliques)
                    sh 'npm install'
                    sh 'npm run build --prod'
                }
            }
        }

        stage('Build Backend') {
            steps {
                dir('backend') {
                    sh 'pip install -r requirements.txt'
                }
            }
        }

        stage('Tests Frontend') {
            steps {
                dir('frontend') {
                    sh 'npm test -- --watch=false --no-progress'
                }
            }
        }

        stage('Tests Backend') {
            steps {
                dir('backend') {
                    sh 'pytest tests/ --maxfail=1 -v'
                }
            }
        }

        stage('Deploy') {
            steps {
                echo '🚀 Déploiement terminé avec succès !'
            }
        }
    }

    post {
        success {
            echo '✅ Build réussi !'
        }
        failure {
            echo '❌ Build échoué - Vérifiez les logs'
        }
    }
}